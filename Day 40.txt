s_username="EMC"
s_password=123

def validate(username,password):
    if(username==s_username and password==s_password):
        return True
    else: 
        return False
a = validate(username=input("Username: "),password=int(input("Password: ")))
print(a)